export interface User {
  userId: string;
  name: string;
  username: string;
  color: string;
  avatar: string;
}

export interface Club {
  id: string;
  name: string;
  color: string;
  memberCount: number;
} 